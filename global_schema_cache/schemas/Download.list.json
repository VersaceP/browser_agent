{
  "method": "Download.list",
  "description": "Lists recent download records, optionally filtered by ID or lifecycle state.",
  "params": {
    "fleetId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
      "description": "Fleet ID whose downloads should be queried.",
      "required": true
    },
    "downloadId": {
      "description": "Optional opaque download ID to inspect.",
      "type": "string",
      "minLength": 1,
      "required": false
    },
    "states": {
      "description": "Optional lifecycle states to include.",
      "minItems": 1,
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "downloading",
          "paused",
          "completed",
          "failed",
          "cancelled"
        ]
      },
      "required": false
    },
    "limit": {
      "default": 100,
      "description": "Maximum recent download records to return.",
      "type": "integer",
      "minimum": 1,
      "maximum": 1000,
      "required": true
    },
    "purpose": {
      "type": "string",
      "minLength": 1,
      "description": "Describe why you are viewing downloads, e.g., checking if a download has completed",
      "required": true
    }
  },
  "requiresPurpose": true,
  "purposeHint": "Describe why you are viewing downloads, e.g., checking if a download has completed"
}