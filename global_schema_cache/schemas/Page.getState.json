{
  "method": "Page.getState",
  "description": "Synchronizes page state, including URL, title, load status, HITL pause status, and blocking dialogs or file choosers.",
  "params": {
    "pageId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
      "description": "Page ID whose current state should be synchronized.",
      "required": true
    },
    "purpose": {
      "type": "string",
      "description": "Describe why you need state synchronization, e.g., checking load completion, verifying no blocking dialog, or resuming after HITL",
      "required": true
    }
  },
  "requiresPurpose": true,
  "purposeHint": "Describe why you need state synchronization, e.g., checking load completion, verifying no blocking dialog, or resuming after HITL"
}