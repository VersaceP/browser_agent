{
  "method": "System.getCapabilities",
  "description": "Discovers callable actions and the high-level agent SOP guide; use it first, then query unfamiliar action schemas with System.describeAction or event contracts with System.listEvents.",
  "params": {
    "skillFile": {
      "description": "Return the SOP guide as a file path instead of inline content when true.",
      "type": "boolean",
      "required": false
    }
  },
  "requiresPurpose": false
}