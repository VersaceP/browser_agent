{
  "method": "DOM.getText",
  "description": "Reads textContent from one or more known DOM elements by canonical id or selector.",
  "params": {
    "pageId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
      "description": "Page ID containing the target elements.",
      "required": true
    },
    "targets": {
      "minItems": 1,
      "type": "array",
      "items": {
        "anyOf": [
          {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "pattern": "^\\d+:\\d+:\\d+$",
                "description": "Canonical element ID from DOM.getAXTree."
              },
              "selector": {
                "not": {}
              }
            },
            "required": [
              "id"
            ],
            "additionalProperties": false
          },
          {
            "type": "object",
            "properties": {
              "selector": {
                "type": "string",
                "minLength": 1,
                "description": "CSS selector for a target element in the main frame."
              },
              "id": {
                "not": {}
              }
            },
            "required": [
              "selector"
            ],
            "additionalProperties": false
          }
        ],
        "description": "DOM query target identified by either canonical id or selector."
      },
      "description": "Targets to read, preserving input order in response.data.items.",
      "required": true
    },
    "purpose": {
      "type": "string",
      "description": "Describe why this element text is needed, e.g., verifying a success message, reading a result section, or extracting submitted feedback",
      "required": true
    }
  },
  "requiresPurpose": true,
  "purposeHint": "Describe why this element text is needed, e.g., verifying a success message, reading a result section, or extracting submitted feedback"
}