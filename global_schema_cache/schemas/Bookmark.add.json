{
  "method": "Bookmark.add",
  "description": "Adds a new bookmark.",
  "params": {
    "url": {
      "type": "string",
      "format": "uri",
      "description": "Bookmark target URL.",
      "required": true
    },
    "title": {
      "type": "string",
      "description": "Bookmark title to save.",
      "required": true
    },
    "faviconUrl": {
      "description": "Optional favicon URL to store with the bookmark.",
      "type": "string",
      "required": false
    },
    "folderId": {
      "description": "Destination folder ID; use null or omit for the root folder.",
      "anyOf": [
        {
          "type": "integer",
          "minimum": -9007199254740991,
          "maximum": 9007199254740991
        },
        {
          "type": "null"
        }
      ],
      "required": false
    },
    "purpose": {
      "type": "string",
      "description": "Describe why you are adding a bookmark, e.g., saving an important page for later",
      "required": true
    }
  },
  "requiresPurpose": true,
  "purposeHint": "Describe why you are adding a bookmark, e.g., saving an important page for later"
}