{
  "method": "Memory.delete",
  "description": "Deletes agent-scoped task memory for a scope; use only when a task context is obsolete, not for clearing page data or browser state.",
  "params": {
    "scope": {
      "type": "string",
      "minLength": 1,
      "description": "Memory scope key to delete.",
      "required": true
    }
  },
  "requiresPurpose": false
}