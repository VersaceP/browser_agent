{
  "method": "Memory.save",
  "description": "Saves or updates agent-scoped task context for a scope, including objective, constraints, credentials notes, and milestones; it does not store page content.",
  "params": {
    "scope": {
      "type": "string",
      "minLength": 1,
      "description": "Memory scope key, usually matching the fleet scope.",
      "required": true
    },
    "context": {
      "type": "string",
      "minLength": 1,
      "description": "Free-text task context for this scope, such as goal, constraints, credentials notes, and current milestone.",
      "required": true
    }
  },
  "requiresPurpose": false
}