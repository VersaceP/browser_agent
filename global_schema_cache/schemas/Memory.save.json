{
  "method": "Memory.save",
  "description": "Saves or updates Fleet-global task context with optimistic concurrency; it does not store page content.",
  "params": {
    "fleetId": {
      "type": "string",
      "minLength": 1,
      "description": "Fleet ID whose shared context is being updated.",
      "required": true
    },
    "context": {
      "type": "string",
      "minLength": 1,
      "description": "Free-text task context for this scope, such as goal, constraints, credentials notes, and current milestone.",
      "required": true
    },
    "expectedRevision": {
      "description": "Revision read before editing. Omit only when creating the first record.",
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991,
      "required": false
    }
  },
  "requiresPurpose": false
}