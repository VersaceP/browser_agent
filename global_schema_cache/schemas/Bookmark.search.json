{
  "method": "Bookmark.search",
  "description": "Searches bookmarks by keyword (matches title or URL).",
  "params": {
    "query": {
      "type": "string",
      "minLength": 1,
      "description": "Search text matched against bookmark title or URL.",
      "required": true
    },
    "limit": {
      "default": 200,
      "description": "Maximum bookmark matches to return.",
      "type": "integer",
      "minimum": 1,
      "maximum": 1000,
      "required": true
    },
    "purpose": {
      "type": "string",
      "description": "Describe why you are searching bookmarks, e.g., finding a bookmarked site",
      "required": true
    }
  },
  "requiresPurpose": true,
  "purposeHint": "Describe why you are searching bookmarks, e.g., finding a bookmarked site"
}