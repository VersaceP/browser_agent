{
  "method": "History.search",
  "description": "Searches browsing history by keyword (matches title or URL).",
  "params": {
    "query": {
      "type": "string",
      "minLength": 1,
      "description": "Search text matched against history title or URL.",
      "required": true
    },
    "limit": {
      "default": 200,
      "description": "Maximum history matches to return.",
      "type": "integer",
      "minimum": 1,
      "maximum": 1000,
      "required": true
    },
    "purpose": {
      "type": "string",
      "description": "Describe why you are searching history, e.g., finding a specific previously visited site",
      "required": true
    }
  },
  "requiresPurpose": true,
  "purposeHint": "Describe why you are searching history, e.g., finding a specific previously visited site"
}