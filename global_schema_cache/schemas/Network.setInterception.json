{
  "method": "Network.setInterception",
  "description": "Sets network request interception rules to filter unwanted requests and improve performance.",
  "params": {
    "pageId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
      "description": "Page ID whose network requests should be filtered.",
      "required": true
    },
    "patterns": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "urlPattern": {
            "type": "string",
            "description": "URL match pattern for the request rule."
          },
          "resourceType": {
            "description": "Optional request resource type to narrow the rule.",
            "type": "string"
          },
          "action": {
            "type": "string",
            "enum": [
              "block",
              "allow"
            ],
            "description": "Whether matching requests should be blocked or explicitly allowed."
          }
        },
        "required": [
          "urlPattern",
          "action"
        ],
        "additionalProperties": false
      },
      "description": "Interception rules to apply in order.",
      "required": true
    },
    "purpose": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "string",
      "minLength": 1,
      "description": "Describe why you are intercepting requests, e.g., blocking ad requests to improve performance",
      "required": true
    }
  },
  "requiresPurpose": true,
  "purposeHint": "Describe why you are intercepting requests, e.g., blocking ad requests to improve performance"
}