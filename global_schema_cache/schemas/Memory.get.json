{
  "method": "Memory.get",
  "description": "Retrieves agent-scoped task context, constraints, and milestones for a scope; this is not a page data or browser state query.",
  "params": {
    "scope": {
      "type": "string",
      "minLength": 1,
      "description": "Memory scope key to read.",
      "required": true
    }
  },
  "requiresPurpose": false
}