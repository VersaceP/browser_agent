{
  "method": "Memory.get",
  "description": "Retrieves Fleet-global task context; this is not a page data or browser state query.",
  "params": {
    "fleetId": {
      "type": "string",
      "minLength": 1,
      "description": "Fleet ID whose shared context should be read.",
      "required": true
    }
  },
  "requiresPurpose": false
}