{
  "method": "Workflow.resume",
  "description": "Resumes a paused workflow run so execution can continue.",
  "params": {
    "runId": {
      "type": "string",
      "minLength": 1,
      "description": "Workflow run ID returned by Workflow.execute or supplied by the caller.",
      "required": true
    }
  },
  "requiresPurpose": false
}