{
  "method": "Bookmark.remove",
  "description": "Removes one bookmark or all bookmarks while preserving the folder tree.",
  "params": {
    "fleetId": {
      "type": "string",
      "format": "uuid",
      "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
      "description": "Fleet ID whose bookmark store should be changed.",
      "required": true
    },
    "scope": {
      "type": "string",
      "enum": [
        "item",
        "all"
      ],
      "description": "Removal scope.",
      "required": true
    },
    "id": {
      "description": "Opaque bookmark ID when scope is item.",
      "type": "string",
      "minLength": 1,
      "required": false
    },
    "purpose": {
      "type": "string",
      "minLength": 1,
      "description": "Describe why you are removing this bookmark, set of bookmarks, or folder, e.g., cleaning up unneeded saves",
      "required": true
    }
  },
  "requiresPurpose": true,
  "purposeHint": "Describe why you are removing this bookmark, set of bookmarks, or folder, e.g., cleaning up unneeded saves"
}